# Dependencias de ejecución. **Sólo lo que alguien importa.**
#
# Llevaba diez paquetes que no aparecen en un solo `import` del proyecto
# —python-telegram-bot, pydantic, aiohttp, httpx, APScheduler, sqlite-utils,
# click, rich, tabulate, python-dotenv— más pytest y pytest-asyncio, que metían
# el marco de pruebas en la imagen de producción. El `Dockerfile` instala este
# fichero entero en una `e2-small` con 2 GB para todo, y `CLAUDE.md` pone
# «no añadir dependencias pesadas al camino de arranque» entre lo que no se
# hace. Las de desarrollo viven en `requirements-dev.txt`.

# Core & Runtime
pyyaml>=6.0.2

# LLM & Hermes Integration
# OpenRouter (agregador declarado en hermes_config.yaml) habla el protocolo
# de OpenAI, así que este único SDK cubre toda la cadena de modelos.
openai>=1.35.0

# Vertex AI (Gemini Enterprise Agent Platform). Habla el protocolo de OpenAI,
# así que reutiliza el SDK de arriba; lo único que hace falta aparte son las
# credenciales del proyecto. No se necesita google-cloud-aiplatform.
google-auth>=2.30.0

# Medios de frontera sobre Vertex (src/tools/vertex_media.py). Opcionales: sin
# ellos Yuki sigue arrancando y la pasarela de medios cae a sus marcadores.
#   google-genai            -> Imagen (portadas) y Gemini Omni Flash (vídeo)
#   google-cloud-texttospeech -> Gemini TTS, con salida OGG Opus nativa
google-genai>=1.9.0
google-cloud-texttospeech>=2.16.0

# Adaptador de Discord: el gateway real, y el único que necesita SDK.
# Telegram usa la biblioteca estándar a propósito (ver src/adapters/telegram_bot.py).
discord.py>=2.3.2

# Serverless (opcional para Modal). El módulo lo declara en su cabecera: no lo
# arranca nadie desde el repositorio.
modal>=0.62.0
